import { CcShortLinkDirective } from './cc-short-link.directive';

describe('CcShortLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new CcShortLinkDirective();
    expect(directive).toBeTruthy();
  });
});
